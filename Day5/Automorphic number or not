def is_automorphic(number):
    squared_number = number ** 2
    return str(squared_number).endswith(str(number))
number = 142
if is_automorphic(number):
    print(f"{number} is an automorphic number.")
else:
    print(f"{number} is not an automorphic number.")
