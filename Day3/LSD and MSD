def find_lsd_msd(number)
     number_str=str(number)
     lsd=int(number_str[-1])
     msd=int(number_str[0])
     return lsd,msd
number=12345
lsd,msd=find_lsd_msd(number)
Print(f"least significant digit:{lsd}")
Print(f"most significant digit:{msd}")
